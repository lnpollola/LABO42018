import { PerroGatoDirective } from './perro-gato.directive';

describe('PerroGatoDirective', () => {
  it('should create an instance', () => {
    const directive = new PerroGatoDirective();
    expect(directive).toBeTruthy();
  });
});
