import { DirectivaTiposDirective } from './directiva-tipos.directive';

describe('DirectivaTiposDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivaTiposDirective();
    expect(directive).toBeTruthy();
  });
});
